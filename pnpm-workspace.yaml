packages:
  - app/*
  - lib/*
  - functions

# pnpm 11 requires explicit approval for dependencies that run install scripts.
# Keep this list in sync with `pnpm approve-builds`.
allowBuilds:
  '@firebase/util': true
  esbuild: true
  protobufjs: true
  re2: true
